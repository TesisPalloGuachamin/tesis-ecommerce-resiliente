spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/checkout_test_db
    username: postgres
    password: postgres
  
  jpa:
    hibernate:
      ddl-auto: create-drop
  
  rabbitmq:
    host: localhost
    port: 5672

logging:
  level:
    com.tesis.ecommerce: DEBUG

